<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.danielluzgarten.androidhomework">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeWork2first" />
        <activity android:name=".HomeWork2secund" />
        <activity android:name=".ConstraintLayout1" />
        <activity android:name=".ConstrianLayout2" />
        <activity android:name=".ConstrainLayout3" />
        <activity android:name=".MyBirthdays"
            android:windowSoftInputMode="stateHidden"
            ></activity>

    </application>

</manifest>